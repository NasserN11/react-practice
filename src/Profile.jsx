

const ProfileInfo = ({name, university, country}) => {
    return (
        <div>
            <h2>Name: {name}</h2>
            <p>University: {university} University</p>
            <p>Country: {country}</p>

        </div>
    );
};

export default ProfileInfo;